/*
 * 约定: store中的fetchXXX都是指从后端请求XXX，
 *      getXXX都是指从store中取数据
 */

import {
} from '@/api/prescription'

export default {
  namespaced: true,
  state: {
  },
  mutations: {
  },
  getters: {
  }
}
